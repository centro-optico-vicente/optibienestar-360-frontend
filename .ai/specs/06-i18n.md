# 06 — i18n (Internacionalización)

## Configuración

```typescript
// nuxt.config.ts
modules: ['@nuxtjs/i18n'],
i18n: {
  locales: [
    { code: 'es', name: 'Español', file: 'es.json' },
    { code: 'en', name: 'English', file: 'en.json' },
  ],
  defaultLocale: 'es',
  strategy: 'no_prefix',  // no agregar /es/ a URLs (app SPA tras login)
  lazy: true,
  langDir: 'locales/',
}
```

> **Bilingüe desde el día 1.** El backend ya devuelve `ProblemDetail` y correos
> localizados en `en`, así que el frontend nace con `es` **y** `en` (mismas keys en
> ambos bundles). Los archivos viven en `i18n/locales/{es,en}.json`.

## Estructura de mensajes

`i18n/locales/es.json`:

```json
{
  "common": {
    "save": "Guardar",
    "cancel": "Cancelar",
    "delete": "Eliminar",
    "edit": "Editar",
    "search": "Buscar",
    "filter": "Filtrar",
    "loading": "Cargando...",
    "no_results": "Sin resultados",
    "confirm": "Confirmar",
    "yes": "Sí",
    "no": "No"
  },
  "auth": {
    "login": "Iniciar sesión",
    "logout": "Cerrar sesión",
    "email_or_id": "Email o cédula",
    "password": "Contraseña",
    "forgot_password": "¿Olvidaste tu contraseña?",
    "invalid_credentials": "Credenciales inválidas"
  },
  "members": {
    "title": "Afiliados",
    "new": "Nuevo afiliado",
    "fields": {
      "document_number": "Cédula",
      "full_name": "Nombre completo",
      "email": "Correo electrónico",
      "phone": "Teléfono",
      "address": "Dirección",
      "plan": "Plan",
      "beneficiaries": "Beneficiarios"
    },
    "validations": {
      "document_invalid": "Formato de cédula inválido (V-12345678 o E-12345678)",
      "age_min": "Edad mínima 18 años",
      "max_beneficiaries": "Máximo 3 beneficiarios"
    }
  },
  "memberships": {
    "status": {
      "ACTIVE": "Solvente",
      "SUSPENDED": "Inactivo",
      "EXPIRED": "Vencida",
      "CANCELLED": "Cancelada"
    }
  },
  "payments": {
    "status": {
      "PENDING_REVIEW": "Pendiente de revisión",
      "APPROVED": "Aprobado",
      "REJECTED": "Rechazado"
    },
    "method": {
      "ZELLE": "Zelle",
      "TRANSFERENCIA": "Transferencia",
      "EFECTIVO": "Efectivo"
    },
    "approve": "Aprobar pago",
    "reject": "Rechazar pago"
  },
  "validator": {
    "title": "Validador",
    "search_placeholder": "Ingresá la cédula del afiliado",
    "result_valid": "Afiliado vigente",
    "result_invalid": "Afiliado no vigente",
    "register_usage": "Registrar uso"
  },
  "errors": {
    "generic": "Algo salió mal. Intentá nuevamente.",
    "no_permission": "No tenés permiso para realizar esta acción",
    "not_found": "Recurso no encontrado",
    "network": "Error de conexión. Verificá tu internet."
  }
}
```

## Uso

```vue
<template>
  <h1>{{ $t('members.title') }}</h1>
  <UButton>{{ $t('common.save') }}</UButton>
  <UBadge :color="statusColor">{{ $t(`memberships.status.${status}`) }}</UBadge>
</template>

<script setup>
const { t } = useI18n();
const message = t('payments.approve');
</script>
```

## Reglas

- **Toda string visible al usuario debe estar en `es.json` + `en.json`** (mismas keys) — no hardcodear en componentes
- **Errores de negocio NO se traducen en el cliente** — los `ProblemDetail` del backend ya llegan localizados; en el cliente solo se traducen textos UI-only (labels, botones, títulos, vacíos, validaciones zod)
- **Formato NO se ata al locale de UI** — moneda/fecha vía `useFormatters` siguen la convención `es-VE` aunque la interfaz esté en inglés ([ADR 0010](../decisions/0010-localization-venezuela.md))
- **Nested keys por dominio** — facilita encontrar y mantener
- **NO incluir HTML en strings** — usar componentes para markup
- **Pluralización:** usar Vue I18n syntax `'{count} afiliado | {count} afiliados'`
- **Interpolación:** `'Hola, {name}'` + `t('greeting', { name: 'Juan' })`

## Formato de fechas / números

Usar `useFormatters` composable + i18n:

```typescript
// composables/useFormatters.ts
export const useFormatters = () => {
  const formatCurrency = (amount: number, currency = 'USD') =>
    new Intl.NumberFormat('es-VE', { style: 'currency', currency }).format(amount);

  const formatDate = (date: string | Date, format: 'short' | 'long' = 'short') => {
    const d = typeof date === 'string' ? new Date(date) : date;
    if (format === 'long') {
      return new Intl.DateTimeFormat('es-VE', { day: 'numeric', month: 'long', year: 'numeric' }).format(d);
    }
    return new Intl.DateTimeFormat('es-VE').format(d);
  };

  const formatRelative = (date: string | Date) => {
    // "hace 3 días", "en 2 semanas"
    const rtf = new Intl.RelativeTimeFormat('es-VE', { numeric: 'auto' });
    // ... lógica
  };

  return { formatCurrency, formatDate, formatRelative };
};
```

Uso:
```vue
<script setup>
const { formatCurrency, formatDate } = useFormatters();
</script>
<template>
  <td>{{ formatCurrency(payment.amount) }}</td>
  <td>{{ formatDate(membership.next_due_date, 'long') }}</td>
</template>
```

## Estado actual y siguientes pasos

El inglés **ya no es futuro**: `es` y `en` conviven desde el día 1 porque el backend
devuelve payloads localizados en ambos. Pendiente (ver [`checklists/vertical-11-i18n.md`](../checklists/vertical-11-i18n.md)):

1. ~~Crear `locales/en.json` con mismas keys~~ ✅ hecho
2. ~~Agregar locale al config Nuxt~~ ✅ hecho
3. **`<LocaleSwitcher>` ES/EN en el header** del layout autenticado (Fase 2) — entry point del usuario.
4. **Sincronizar locale con el backend** (Fase 2): leer `user.locale` de `/v1/me`, cambiar vía `POST /v1/me/locale`, mandar `Accept-Language` en cada request (Fase 3).

Si se suma un tercer idioma: crear `locales/<code>.json` con las mismas keys y agregarlo
al array `locales` del config. No se usa `prefix_except_default` (la app es SPA tras login).

## Referencias

- [`@nuxtjs/i18n` docs](https://i18n.nuxtjs.org/)
- [Vue I18n docs](https://vue-i18n.intlify.dev/)
